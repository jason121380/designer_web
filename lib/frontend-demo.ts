import { defaultDesignerWebContent } from "@/lib/designer-web-content";

export const frontendDemo = {
  ...defaultDesignerWebContent,
  brandName: defaultDesignerWebContent.brand.name,
  themeColor: defaultDesignerWebContent.brand.themeColor,
};
